# VerticalSlice.PageProcessor
Реализовал все что перечислено в ТЗ, проверил работу, ошибки. Все работает, контейнеры поднимаются, работа в точности как в ТЗ. C версионированием и ветками в git заморачиваться не стал, старался сделать быстрее
**Для запуска**
****
В папке проекта ввести `docker compose up`, поднимутся все 3 контейнера. Для доступа к pgadmin нужно перейти в `http://localhost:8080`, для доступа к swagger `http://localhost:8090/api/swagger/`.
